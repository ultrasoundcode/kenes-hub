# Быстрый старт - Kenes Hub

## Установка за 5 минут

### Способ 1: Docker (рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone <repository-url>
cd kenes-hub

# 2. Скопируйте настройки
cp .env.example .env

# 3. Отредактируйте .env файл (укажите свои ключи)
nano .env  # или используйте любой редактор

# 4. Запустите установку
chmod +x scripts/setup.sh
./scripts/setup.sh

# 5. Готово! Приложение запущено
# Frontend: http://localhost:3000
# Backend: http://localhost:8000
# Admin: http://localhost:8000/admin
```

### Способ 2: Локальная разработка

#### Backend (Django):

```bash
# 1. Установите зависимости
cd backend
python3.11 -m venv venv
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt

# 2. Настройте базу данных
createdb kenes_hub
python manage.py migrate
python manage.py createsuperuser

# 3. Запустите сервер
python manage.py runserver
```

#### Frontend (Next.js):

```bash
# 1. Установите зависимости
cd frontend
npm install

# 2. Запустите сервер
npm run dev
```

#### Redis:

```bash
# Убедитесь, что Redis запущен
redis-server
```

## Первые шаги

### 1. Войдите в админ-панель

- Перейдите на http://localhost:8000/admin
- Введите учетные данные суперпользователя

### 2. Создайте первую заявку

- Перейдите на http://localhost:3000
- Зарегистрируйтесь или войдите
- Создайте новую заявку

### 3. Попробуйте AI-помощника

- Перейдите в раздел "AI Помощник"
- Задайте вопрос о реструктуризации долгов

### 4. Сгенерируйте документ

- Создайте шаблон документа в админ-панели
- Сгенерируйте документ для заявки
- Подпишите документ (используйте тестовый SMS код: 123456)

## Основные команды

### Docker

```bash
# Запуск
docker-compose up -d

# Остановка
docker-compose down

# Логи
docker-compose logs -f

# Пересборка
docker-compose build --no-cache
```

### Backend

```bash
cd backend
source venv/bin/activate

# Миграции
python manage.py makemigrations
python manage.py migrate

# Тесты
python manage.py test

# Создание суперпользователя
python manage.py createsuperuser
```

### Frontend

```bash
cd frontend

# Запуск
npm run dev

# Сборка
npm run build

# Тесты
npm test
```

## Типичные проблемы

### "Port already in use"

```bash
# Проверьте, что занимает порт 8000
sudo lsof -i :8000

# Или используйте другой порт
python manage.py runserver 0.0.0.0:8001
```

### "Database connection failed"

```bash
# Проверьте PostgreSQL
sudo systemctl status postgresql  # Linux
brew services list | grep postgresql  # macOS

# Запустите PostgreSQL
sudo systemctl start postgresql  # Linux
brew services start postgresql   # macOS
```

### "Module not found"

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # macOS/Linux
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt
```

## Дальнейшие шаги

1. **Прочитайте полную документацию** в [DEPLOYMENT_GUIDE.md](DEPLOYMENT_GUIDE.md)
2. **Изучите руководство разработчика** в [DEVELOPER_GUIDE.md](DEVELOPER_GUIDE.md)
3. **Поэкспериментируйте с API** на http://localhost:8000/api/docs/
4. **Настройте интеграции** (WhatsApp, SMS, Email)
5. **Добавьте свои шаблоны документов**

## Получение помощи

- 📖 Документация: [README.md](README.md)
- 🐛 Issues: Создайте issue на GitHub
- 💬 Чат: Присоединитесь к командному чату
- 📧 Email: support@kenes-hub.kz

## Готово! 🎉

Вы успешно установили Kenes Hub. Теперь вы можете:
- ✅ Создавать и управлять заявками
- ✅ Генерировать документы
- ✅ Использовать AI-помощника
- ✅ Отправлять уведомления
- ✅ Подписывать документы электронной подписью

Приятной работы!